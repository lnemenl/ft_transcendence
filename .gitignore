node_modules/

# We only commit the source code (TypeScript), not the compiled JavaScript.
dist/
build/

# Logs
# Log files are generated at runtime and should not be versioned.
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pids/
*.pid
*.seed
*.pid.lock

# Environment variables
# CRITICAL: This is a major security rule. Never commit your secrets.
.env
.env.*
# Allow the example environment file to be committed.
!.env.example

# Database
# Database files are runtime data, not source code.
*.db
*.db-journal

# OS generated files
# These are system-specific and have no place in a shared repository.
.DS_Store
Thumbs.db
ehthumbs.db

# Editor directories and files
# Prevents editor-specific settings from cluttering the repository.
.vscode/*
# It's a good practice to share settings for consistent code style.
!.vscode/settings.json
# It's also good to share recommended extensions for the project.
!.vscode/extensions.json

.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Test and coverage reports
# These are generated when you run tests and should not be committed.
coverage/
.nyc_output/